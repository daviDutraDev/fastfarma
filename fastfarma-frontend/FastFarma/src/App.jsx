import './App.css'

function App() {
  // O roteamento e a árvore de páginas são montados em `main.jsx`.
  // Este componente existe apenas para satisfazer o `createRoot` quando
  // o app é construído sem o entry principal (testes isolados, etc).
  return <div />
}

export default App
